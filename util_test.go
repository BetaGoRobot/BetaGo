package main

import "testing"

func TestGetCurrentTime(t *testing.T) {
	GetCurrentTime()
}

func TestSomething(t *testing.T) {
	sampleStr := "/search closer"
	if res := reg.FindStringSubmatch(sampleStr); res != nil && len(res) != 0 {

	}
}
